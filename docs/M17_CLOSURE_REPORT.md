# M17C closure — Philippine Stock Exchange Tower

Status: **CLOSED_WITH_DEFERMENT** for v1. The active M17 batch contains PSE only; nine historical candidates remain in a separate deferred backlog. M16R evidence readiness remains PASS, but readiness is not visual approval.

The final bounded attempt added one massing round and zero facade rounds. Deterministic [OSM part analysis](../data/reports/pse-part-analysis.json) found the 131 m footprint fully nested in the 119.2 m footprint. The builder partitions the overlapping footprint and retains the geographic anchor. This is an **inferred architectural interpretation** of verified geographic polygons, not proof of the physical tower structure. Component top forms are explicit flat inferred placeholders; the unsupported universal 4 m roof slope is gone. The principal ribbed facade edge could not be identified uniquely from the two southeast source views, so facade geometry stays disabled.

The two approximate camera matches to Handel images 044a and 008a still fail source comparison. Four major discrepancies remain open: overall silhouette, relative body/frontpiece volume, roof form, and facade composition. Zero critical issues are confirmed. No independent visual signoff exists. The current draft GLB structurally validates (9,960 bytes, 120 triangles, 3 meshes, 2 materials) but is **unpublished**. Its LOD transition was not run because the asset did not pass visual QA. The mapped LOD2 fallback remains active.

Image 045 was inspected as a distant context view; it did not resolve the upper form, opposite facade, or principal edge and was not promoted to a camera match. Research photographs remain remote and research-only. More iterations on the same two southeast views would not justify speculative architecture for v1.

Production remains at seven approved LOD1 assets, 6,982 canonical LOD2 buildings, and 94 tiles. No failed M17 draft is published. [Reference QA](M17_REFERENCE_QA.md), [reconstruction record](M17_RECONSTRUCTION_REPORT.md), and [M18 acceptance](M18_ACCEPTANCE_REPORT.md) contain the supporting decisions.
